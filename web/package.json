{
  "name": "assistant-pi",
  "version": "0.1.0",
  "description": "Frontent for assistant pi",
  "scripts": {
    "test": "spago test",
    "start": "vite src",
    "build": "vite build src --outDir ../dist --emptyOutDir",
    "preview": "vite preview src"
  },
  "author": "James Leslie",
  "license": "MIT",
  "dependencies": {
    "@patternfly/patternfly": "^5.4.0"
  },
  "devDependencies": {
    "htmlparser2": "^9.1.0",
    "vite": "^5.4.5"
  }
}
